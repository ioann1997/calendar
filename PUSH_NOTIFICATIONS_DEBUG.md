# Диагностика Push-уведомлений

## Как проверить, почему не приходят push-уведомления

### 1. Проверьте консоль браузера

Откройте DevTools (F12) и проверьте логи с префиксом `[FCM]`:

**Ожидаемые логи при успешной инициализации:**
```
[FCM] Начало инициализации Firebase Cloud Messaging
[FCM] Текущее разрешение: granted
[FCM] Firebase Messaging инициализирован
[FCM] Service Worker готов
[FCM] Получение FCM токена...
[FCM] ✅ Токен успешно получен: ...
[FCM] ✅ Токен сохранен в Firebase. Push-уведомления должны работать.
```

**Возможные проблемы:**

#### ❌ "Разрешение на уведомления не предоставлено"
- **Решение:** Разрешите уведомления в настройках браузера
  - Chrome: Настройки → Конфиденциальность и безопасность → Уведомления
  - Firefox: Настройки → Приватность и защита → Разрешения → Уведомления

#### ❌ "calendarId не установлен, токен не сохранен"
- **Решение:** Обновите страницу. calendarId должен быть в URL: `?c=...`

#### ❌ "Не удалось получить токен"
- **Решение:** 
  - Проверьте, что Service Worker зарегистрирован
  - Проверьте конфигурацию Firebase в `firebase-config.js`
  - Убедитесь, что сайт открыт по HTTPS (или localhost)

### 2. Проверьте, что токен сохранен в Firebase

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект `calendar-b87ed`
3. Перейдите в Firestore Database
4. Найдите документ в коллекции `calendars` с вашим `calendarId`
5. Проверьте поле `fcmTokens` - там должен быть массив с токенами

**Если токенов нет:**
- Проверьте логи в консоли браузера
- Убедитесь, что calendarId правильный
- Попробуйте обновить страницу

### 3. Проверьте, что Firebase Cloud Function развернута

Push-уведомления отправляются через Firebase Cloud Function `checkAndSendReminders`, которая должна быть развернута.

**Проверка:**
```bash
cd functions
firebase functions:list
```

Должна быть функция `checkAndSendReminders`.

**Если функции нет, разверните её:**
```bash
cd functions
npm install
firebase deploy --only functions
```

### 4. Проверьте логи Firebase Functions

```bash
firebase functions:log
```

Ищите логи с префиксом `🦉 Проверка напоминаний...`

**Ожидаемые логи:**
```
🦉 Проверка напоминаний...
⏰ UTC время: ..., Europe/Moscow время: ...
✅ Ежедневный ритуал "...": отправлено X уведомлений
```

### 5. Проверьте настройки напоминаний

1. Убедитесь, что у задачи включено напоминание (чекбокс "Включить напоминание")
2. Установлено время напоминания
3. Задача не выполнена (не отмечена как выполненная)

### 6. Тестовая проверка

**Локальные уведомления (когда приложение открыто):**
- Работают через функцию `checkReminders()` каждую минуту
- Проверьте логи `[Reminders]` в консоли

**Push-уведомления (когда приложение закрыто):**
- Работают через Firebase Cloud Function `checkAndSendReminders`
- Функция запускается каждую минуту по расписанию
- Отправляет уведомления через Firebase Cloud Messaging

### 7. Часовой пояс

Убедитесь, что часовой пояс в `functions/index.js` правильный:
```javascript
timeZone: 'Europe/Moscow', // ⚠️ ИЗМЕНИ НА СВОЙ ЧАСОВОЙ ПОЯС
```

### 8. Частые проблемы

#### Уведомления не приходят на мобильном устройстве
- Проверьте, что приложение установлено как PWA
- Проверьте настройки уведомлений в системе
- Убедитесь, что браузер поддерживает push-уведомления

#### Уведомления приходят с задержкой
- Firebase Cloud Functions запускаются каждую минуту
- Задержка может быть до 1 минуты

#### Токен не сохраняется
- Проверьте права доступа к Firestore
- Убедитесь, что правила безопасности Firestore разрешают запись

## Контакты для помощи

Если проблема не решена:
1. Соберите все логи из консоли браузера
2. Проверьте логи Firebase Functions
3. Убедитесь, что все шаги выше выполнены

